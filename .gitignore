.DS_Store
*.swp
*.swo
*~
node_modules/
.last-update-check

# Project-local state (generated by /svrnty-team-setup:init and :doctor)
# Note: CLAUDE.md and AGENTS.md are tracked in the plugin repo itself
# but are gitignored in target projects via their own .gitignore
.beads/
.overstory/
.mulch/
.gitattributes
