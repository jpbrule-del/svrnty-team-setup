# svrnty teardown — Orchestration Teardown

Merge agent branches, sync tasks and knowledge, clean up worktrees.

## Usage

Run the teardown script:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/teardown.sh"
```

Run this single command and present the output to the user. Do NOT run any other commands — the script handles everything.
